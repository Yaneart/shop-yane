export {
  toggleCategory,
  setPriceMin,
  setPriceMax,
  toggleSize,
  resetFilters,
  selectFilters,
  selectFilteredProducts,
} from './model/filterSlice';

export { default as filterReducer } from './model/filterSlice';
